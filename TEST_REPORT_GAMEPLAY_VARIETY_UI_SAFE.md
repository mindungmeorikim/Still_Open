# 플레이 요소 확장 + UI 안전 패치 검수 보고서

## 자동 검수 통과

- 전체 JavaScript `node --check` 문법 검사 통과
- 전체 상대경로 import 존재 검사 통과
- 서비스워커 APP_SHELL 경로 존재 검사 통과
- Day 1~3 손님 수/간격/동시 활성 프로필 검사 통과
- 시간대 수요 `opening → midday → late` 전환 검사 통과
- 손님 성향 4종 가중 선택 검사 통과
- 청결 중시 손님의 위생 부족 퇴장·안내 문구 검사 통과
- Day 1~3 영업 중 목표 생성 검사 통과
- 목표 달성 시 200골드 지급 및 중복 지급 차단 검사 통과
- Day 1 돌발 상황 40% 확률 분기와 Day 2 이후 발생 조건 검사 통과
- 냉장고 돌발 상황 생성·해결·통계 저장 검사 통과
- 추천 상품 판매 통계 및 정산 하이라이트 최대 3줄 검사 통과
- `storePlayFeatures` 저장 스냅샷 포함 검사 통과
- 일일 미션 안내 문구와 목표 카드 중복 출력 방지 검사 통과
- 신규 UI CSS에서 ellipsis 미사용 검사 통과
- 자동 글자 축소 함수 동작 및 `text-overflow: clip` 검사 통과
- 정산 오른쪽 영역의 체크 목록/하이라이트 flex 분리 검사 통과
- PWA 캐시 버전 및 신규 시스템 파일 등록 검사 통과

## 수동 플레이 확인 항목

아래는 실제 기기 폰트와 터치 영역 영향을 받으므로 Live Server 및 BlueStacks에서 확인하세요.

1. 16:9에서 일일 미션창의 목표 카드가 기존 미션 카드를 밀어내지 않는지
2. 16:9에서 정산 하이라이트 3줄이 확인 버튼과 겹치지 않는지
3. 긴 토스트 안내가 잘리지 않고 글자만 자연스럽게 축소되는지
4. 손님 머리 위 성향 아이콘이 말풍선 또는 다른 손님과 과도하게 겹치지 않는지
5. 냉장고 문 경고 아이콘과 냉장고 상호작용 위치가 일치하는지
6. 바닥 오염이 기존 청소 상호작용으로 정상 해결되는지
7. 대량 구매 2~3개가 재고·매출·정산 수량에 동일하게 반영되는지

## 확인 시 주의

- 새 UI 틀은 만들지 않았습니다.
- 기존 좌표, 충돌 박스, 손님 접근점은 변경하지 않았습니다.
- 냉장고 돌발 상황은 기존 냉장고 상호작용 범위만 재사용합니다.
- 서비스 콤보는 포함하지 않았습니다.
